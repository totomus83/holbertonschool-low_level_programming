#include "main.h"
/**
 * _abs - function that print the absolute value of an integer
 * @i: number which will transform
 * Return: return 0 if success
 */
int _abs(int i)
{
	if (i > 0)
		return (i);
	else
		return (-i);
}
